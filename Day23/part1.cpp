#include <fstream>
#include <iostream>
#include <string>
#include <vector>
#include <unordered_set>

#define INPUT_STRING std::string("389125467")
#define NUM_TURNS 10

using namespace std;

int main()
{
    /*vector<int> v;
    for(int i = 0; i < INPUT_STRING.length(); i++) {
        v.push_back(INPUT_STRING[i] - '0');
    }


    int numTurns = 0;
    while(numTurns < NUM_TURNS) {
        int offset = numTurns % v.size();

        int curNumber = v[offset];
        auto itInit = v.begin() + offset;
        auto itEnd = itInit + 3;
        unordered_set<int> nums(itInit, itEnd);
        v.erase(itInit, itEnd);

        numTurns++;
    }*/

    return 0;
}